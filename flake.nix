{
  description = "savau's flakey xmonad configuration. Source: https://github.com/savau/xmonad-config";

  inputs = {
    nixpkgs.url = github:NixOS/nixpkgs/nixos-unstable;
  };

  outputs = { self, nixpkgs }: {
  };
}
